# Backend Test for TH interview

## Dependencies

The application in written and tested running on python version 3.8.6. Compatibility with other versions might differ.

- Flask
- Requests

## How to run

Doing `flask run` or just running it via `python app.py` should run the program without issues.